---
- name: create multiple users using loop
  hosts: all
  become: yes
  
  vars:
    users:
      - mike
      - john
      - kyle

  tasks:
  - name: loop on users 
    user:
      name: "{{ item }}"
      state: present
    loop: "{{ users }}"
